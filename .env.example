# Optional. Copy to .env at the repo root to enable real AI drafting.
# Left unset, the draft endpoint returns available: false and the
# leadership screen falls back to a manually written draft.
ANTHROPIC_API_KEY=
